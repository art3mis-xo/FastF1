# # routers/standings.py
# @router.get("/wdc/{year}")
# async def wdc_standings(year: int): ...     # driver championship

# @router.get("/wcc/{year}")
# async def wcc_standings(year: int): ...     # constructor championship

# @router.get("/race-results/{year}/{gp}")
# async def race_results(year: int, gp: str): ...

# @router.get("/session-results/{year}/{gp}/{session}")
# async def session_results(year: int, gp: str, session: str): ...
# # session = FP1, FP2, FP3, Q, SQ, R, S (sprint)